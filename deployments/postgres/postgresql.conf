# VKino PostgreSQL main config
#
# Этот файл подключается контейнером PostgreSQL как основной config_file.
# Все проектные настройки вынесены в conf.d/vkino.conf.

include_if_exists = '/etc/postgresql/conf.d/vkino.conf'
hba_file = '/etc/postgresql/pg_hba.conf'
